<header class="mb-auto">

  <div class="d-flex justify-content-between align-items-center">
    <h3 class="float-md-start mb-0 "><a class="btn fs-2 text-white fw-bold" href="/">Dashboard X</a> </h3>


<% if (user) { %>
  <div class="d-flex align-items-center">
 <h5 class="m-2 mb-2.5 me-3">welcome : <%= user.username %></h5>
 <a class=" fw-bold btn btn-light " href="./signout">SignOut</a>
</div>
<% } else { %>

      <nav class="nav nav-masthead justify-content-center float-md-end">
        <a class="fw-bold btn <%= currentPage =="login"? "btn-light" : null %> " href="./login">LogIn</a>
        <a class=" fw-bold btn <%= currentPage =="signup"? "btn-light" : null %> " href="./signup">SignUp</a>
      </nav>
    <% } %>

    </div>
  </header>